import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0xFF6B4EFF);
  static const secondary = Color(0xFF00C6AE);
  static const background = Color(0xFFF5F6FA);
  static const surface = Color(0xFFFFFFFF);
}
